#include "nary_trees.h"

/**
 * path_exists - checks if a path exists in an N-ary tree.
 * @root: pointer to the root node of the tree
 * @path: NULL terminated array of strings.
 *
 * Return: 1 if all elements of path are present in tree and if for an element
 * of path at the index i is present in one of the nodes at the depth i.
*/
int path_exists(nary_tree_t const *root, char const * const *path)
{
	(void)root;
	(void)path;

	return (0);
}
