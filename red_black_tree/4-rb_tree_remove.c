#include "rb_trees.h"

/**
 * rb_tree_remove - remove node from RB tree
 * @root: pointer to the root node of tree
 * @n: value to search and remove from tree
 *
 * Return: pointer to the new root of tree after deletion
*/
rb_tree_t *rb_tree_remove(rb_tree_t *root, int n)
{
	(void)root;
	(void)n;

	return (NULL);
}
