#include "rb_trees.h"

/**
 * array_to_rb_tree - build a RB Tree from array
 * @array: pointer to the first element of the array to be converted
 * @size: number of elements in array
 *
 * Return: pointer to root node of new R-B tree, or NULL on failure
*/
rb_tree_t *array_to_rb_tree(int *array, size_t size)
{
	(void *)array;
	(void *)size;

	return (NULL);
}
