#include <stdio.h>
#include <stdlib.h>

#include "huffman.h"

/**
 * huffman_priority_queue - creates priority queue for the Huffman coding
 * @data: an array of characters of size size
 * @freq: array containing associated frequencies (of size size too)
 * @size: size of data and freq
 *
 * Return: pointer to the created min heap (also called priority queue)
*/
heap_t *huffman_priority_queue(char *data, size_t *freq, size_t size)
{
	(void)data;
	(void)freq;
	(void)size;

	return (NULL);
}
