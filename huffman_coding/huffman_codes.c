#include <stdio.h>
#include <stdlib.h>

#include "huffman.h"

/**
 * huffman_codes - build tree and print resulting Huffman codes for symbol
 * @data: an array of characters of size size
 * @freq: array containing associated frequencies (of size size too)
 * @size: size of data and freq
 *
 * Return: 1 if it succeed, 0 if it fails
*/
int huffman_codes(char *data, size_t *freq, size_t size)
{
	(void)data;
	(void)freq;
	(void)size;

	return (0);
}
