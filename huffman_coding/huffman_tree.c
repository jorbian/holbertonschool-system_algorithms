#include <stdio.h>
#include <stdlib.h>

#include "huffman.h"

/**
 * huffman_tree - function that builds the Huffman tree
 * @data: an array of characters of size size
 * @freq: array containing the associated frequencies (of size size too)
 * @size: number of symbols being dealt with
 *
 * Return: root node of the Huffman tree, or NULL if it fails
*/
binary_tree_node_t *huffman_tree(char *data, size_t *freq, size_t size)
{
	(void)data;
	(void)freq;
	(void)size;

	return (NULL);
}
