#include <stdio.h>
#include <stdlib.h>

#include "heap.h"

/**
 * heap_extract - extracts root value of a Min Binary Heap
 * @heap: pointer to heap from which to extract value
 *
 * Return: pointer to data stored in root node of heap
*/
void *heap_extract(heap_t *heap)
{
	(void)heap;

	return (NULL);
}
